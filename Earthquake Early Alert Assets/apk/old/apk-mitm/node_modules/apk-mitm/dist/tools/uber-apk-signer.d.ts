import Tool from './tool';
export default class UberApkSigner extends Tool {
    sign(inputPaths: string[], { zipalign }?: {
        zipalign?: boolean | undefined;
    }): import("rxjs").Observable<string>;
    name: string;
    get version(): {
        name: string;
        downloadUrl: string;
    };
}
