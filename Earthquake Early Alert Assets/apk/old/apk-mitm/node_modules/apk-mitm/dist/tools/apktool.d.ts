import Tool from './tool';
interface ApktoolOptions {
    frameworkPath: string;
    customPath?: string;
}
export default class Apktool extends Tool {
    private options;
    constructor(options: ApktoolOptions);
    decode(inputPath: string, outputPath: string): import("rxjs").Observable<string>;
    encode(inputPath: string, outputPath: string, useAapt2: boolean): import("rxjs").Observable<string>;
    private run;
    private get path();
    name: string;
    get version(): {
        name: string;
        downloadUrl?: undefined;
    } | {
        name: string;
        downloadUrl: string;
    };
}
export {};
